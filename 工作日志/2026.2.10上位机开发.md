# Session Bootstrap Context

  ## 1. 当前对话主题

  在 uppper 项目中完成“调度算法 JSON 可视化编辑”链路：main_window 只做 UI，action_server 负责 JSON 读写并回传。


  - 已在调度算法页新增与“开始执行”并列的“打开JSON配置”入口。
  - 点击后不再切换主页面，而是弹出子窗口（QDialog）显示 JSON 编辑页（含加载/保存/返回）。
  - JSON 获取与保存已走既有主从链路：
      - main_window -> action_server 发送请求
      - action_server -> main_window 返回路径/内容/保存结果
  - 已修复“保存后 JSON 布局被打乱”问题：
      - 保存时仅做 json.loads 校验，再按原文本写回（不重排格式）。
  - 构建脚本 build.ps1 已改为产出：
      - launcher/main.exe
      - launcher/clustercomm_release.zip（含 main.exe + common）

  ## 3. 已达成共识 / 规则

  - 架构规则：main_window 只负责 UI 和信号，业务文件操作在 action_server 执行。
  - 路径规则：必须使用相对目录基准（以 uppper 为根），不能写死绝对盘符。
  - 交付规则：不能只发 main.exe，需连同 common/ 一起打包交付。
  - 调度配置认知：当前按“调度算法配置文件”处理（你要求以现有调度文件为准）。


      - 两个并列按钮视觉是否完全对称
      - JSON 弹窗体验是否符合“提示框感”
      - algorithm_control.json 在目标机器上读写是否正常
  - 若后续现场要求“只发一个 exe”，还需再做“首次自动落盘配置”的方案（目前未做）。

  ## 5. 当前进度 / 阶段定位

  落地期（主链路已实现，当前是现场验收与细节微调阶段）。

  ## 6. 技术环境

  - 平台：Windows + PowerShell
  - 项目路径：D:\Work\procedure_code\Bitbucket\clustercomm\uppper
  - 语言/框架：Python + PyQt5
  - 打包：PyInstaller（build.ps1）
  - 关键文件：
      - main_window.py
      - action_server.py
      - ui_files/algorithm_json_editor.ui
      - build.ps1

  ## 7. 输出偏好

  - 直接面向改动和结果，不重复基础概念。
  - 以“可落地、可验证”的最小必要信息为主。
  - 优先给出明确文件位置、链路、下一步动作。