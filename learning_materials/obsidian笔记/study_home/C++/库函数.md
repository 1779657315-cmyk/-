## 1.array
```cpp
#include <array>
```

![[Pasted image 20260827114603.png]]
## 2.map
```cpp
#include <map>
```
![[QQ_1787885661059.png]]
![[QQ_1787885792287.png]]
## 3.chrono
```cpp
#include <chrono>

// 获取当前时间点
auto start = std::chrono::steady_clock::now();

// 获取结束时间
auto end = std::chrono::steady_clock::now();

// 计算耗时
auto cost = std::chrono::duration_cast<std::chrono::milliseconds>(
    end - start
);

std::cout << cost.count() << " ms" << std::endl;
```
常用API：
```cpp
std::chrono::steady_clock::now()   // 获取时间点
std::chrono::milliseconds          // 毫秒
std::chrono::seconds               // 秒
std::chrono::duration_cast         // 时间单位转换
.count()                           // 获取具体数值
```
